#!/bin/sh
set -e
BUILD="$(pwd)"

OPTIONS=""

if "$BUILD"/scripts/test-libmpv ; then
    OPTIONS="$OPTIONS --with-pic"
fi

cd "$config_fribidi_source"
./bootstrap
./configure --prefix="$config_local_prefix" --libdir="$config_local_prefix/lib" --enable-static --disable-shared --without-glib $OPTIONS "$@"
